import ConsoleForm from "./ConsoleForm";

const Home = () => {
  //const [newConsole, setNewConsole] = useState<ConsoleInsertProps | null>(null);
  
  return (
    <ConsoleForm/>
  );
};

export default Home;
